{% extends 'base.html.twig' %}

{% block title %}Cv index{% endblock %}

{% block main %}
    <h1>Cv index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Name</th>
                <th>First_name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {# {% for cv in cvs %} #}
            <tr>
                <td>{{ cv.id }}</td>
                <td>{{ cv.nom }}</td>
                <td>{{ cv.name }}</td>
                <td>{{ cv.first_name }}</td>
                <td>
                    {# <a href="{{ path('app_profile_c_v_show', {'id': cv.id}) }}">show</a> #}
                    <a href="{{ path('app_profile_c_v_edit', {'id': cv.id}) }}">edit</a>
                </td>
            </tr>
        {# {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %} #}
        </tbody>
    </table>

    {# <a href="{{ path('app_profile_c_v_new') }}">Create new</a> #}
{% endblock %}
