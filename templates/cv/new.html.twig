{% extends 'base.html.twig' %}

{% block title %}New Cv{% endblock %}

{% block main %}
    <h1>Create new Cv</h1>

    {{ form_start(form) }}
    {# ... #}

    {{ form_row(form) }}
{{ form_end(form) }}

    <a href="{{ path('app_cv_index') }}">back to list</a>
{% endblock %}
