{% extends 'base.html.twig' %}

{% block title %}Cv{% endblock %}
                                              {# je dois  donner un style à la page #}
{% block main %}
    <h1>Cv</h1>



    {<table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cv.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cv.nom }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cv.name }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td><a href="{{ asset('uploads/curriculumVitae/' ~ cv.first_name) }}">View brochure (PDF)</a></td>
            </tr>
            
        </tbody>
    </table> 

    <a href="{{ path('app_cv_index') }}">back to list</a>

    <a href="{{ path('app_cv_edit', {'id': cv.id}) }}">edit</a>

    {{ include('cv/_delete_form.html.twig') }} 


 {# <div class="card" style="width: 10rem">
            <img src="{{ asset('media/') ~ cv.first_name }}" class="card-img-top" alt="..." />
            <div class="card-body">
                <h5 class="card-title">{{ cv.nom }}</h5>
                <p class="card-text">{{ cv.name }}</p>
                {# <a href="{{ path('app_cv_show', { id: cv.id }) }}" class="btn btn-primary">show</a>
                <a href="{{ path('app_cv_edit', { id: cv.id }) }}" class="btn btn-primary">edit</a> #}
            </div>
        </div> #}


{% endblock %}
